version: '3'
services:
  web:
    image: fitnesstrackerimage
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app
    working_dir: /app
    command: python app.py
  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: melih123
      MYSQL_DATABASE: fitnesstrackerdb
    ports:
      - "3308:3306"
    volumes:
      - ./schema.sql:/docker-entrypoint-initdb.d/schema.sql